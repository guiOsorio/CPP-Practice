#include <iostream>
#include <vector>

using namespace std;

void execute_main();
char display_menu();
bool display_action(const char, vector<int>&);
int print_elements(vector<int>&);
void add_to_vector(int num, vector<int>&);
int calculate_mean(vector<int>&);
int smallest_number(vector<int>&);
int largest_number(vector<int>&);